using UnityEngine;
public class CardController : MonoBehaviour
{
    /*void OnMouseDown()
    {
        if (GameController.instance.cardFlipping && GameController.instance.choosenCard2 != null) return;
        Card card = GetComponent<Card>();
        GameController.instance.FlipCard(card);
        GameController.instance.ChooseCard(card);
    }*/
    
}
